# depends on

Let's add a "depends" to cards, so we can do dependency chains and chain blocking status.

It's either a string or a list of strings, which are the IDs of other cards. If the list is made empty then it gets deleted.
