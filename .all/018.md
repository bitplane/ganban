---
labels:
- data
---

# change .all location

Not sure I like .all anymore.

Need to discuss alternatives. maybe `data` as a dir that gets skipped? Or `cards`?

API change so we'll need a new 0.x version
