# card creation timestamp

Cards need a stable creation timestamp.

* we need a helper to get the creation stamp of a file in a git branch
* we need to make card creation synchronous - it gets added to git immediately
