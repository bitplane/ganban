---
labels:
- bug
---

# default board differs between TUI and init

`app.py:96` `_load_board` copy-pastes the default-board scaffold from `cli/init.py:28-38` and has already diverged: init sets `backlog.meta.compact = True`, the TUI path does not. Extract one shared scaffold helper so both create the same board.
