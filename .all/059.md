# sync column updates

Looks like watchers aren't firing when we update the column (or all markdown?) models

Are they plumbed into the data model? Looks like the model updates. Or does it? Take a look and let's discuss.
