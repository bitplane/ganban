---
deps:
- '25'
labels:
- ui
---

# dependency graph

We should add a tab that shows deps as a diagram.

There's a decent example in Grok CLI's codebase IIRC. We could port that into a Python module for graphs in Textual. Or maybe we could do a full graphviz port and support Mermaid on top of that, and use it to make nice graphs in here.

Worth a discussion - stop before picking this one up and we'll chat
