# Column dragging screen space

Dragging columns around needs to respect scrollbars in the scroll region. it's in screen space at the moment not relative to the board. This causes weird behaviour when scrolled to the right
