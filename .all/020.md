# sync - tree and background

Now that we have a CLI, it'll be a good idea to get the background sync thing working.

To get this going we'll need some kind of sync indicator on the board title, and when you click it you get a drop-down menu to manually sync, disable auto-sync

I'm thinking it's like the auto-update thing that tensorboard has maybe? Not sure. Let's discuss before implementing
