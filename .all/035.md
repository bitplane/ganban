# column view mode

The backlog is getting quite long. Let's add column.meta.view which defaults to "card", and lets the user switch between ways to view the cards.

Setting it to "compact" compacts the cards into "one card per line" - basically no header or footer on the cards themselves, and no gap between them.

Let's discuss this before implementing as it might need some magic to make it work.
