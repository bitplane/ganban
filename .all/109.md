---
labels:
- cleanup
---

# remove dead code and leftovers

- `loader.file_creation_date` — no production callers, only tests import it
- `ContextMenu.ItemSelected` — posted at menu.py:350/398 but no handler exists; consumers use dismiss(result)
- `__main__.NOUNS` lists "serve" but the subcommand is "web" — rename leftover
- `watcher._watches` annotated as list of tuples but stores unwatch callables
