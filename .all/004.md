---
labels:
- web
---

# web ui

Let's add the web server option.

We'll use textual's webserver to serve, print the port out when we open it up. Let the user override the host and port, but default to something sensible that won't clash with other things.
